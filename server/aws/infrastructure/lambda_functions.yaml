AWSTemplateFormatVersion: '2010-09-09'
Description: Lambda Functions + IAM Role for UnitedRelief Platform

Resources:

  LambdaExecutionRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: UnitedReliefLambdaExecutionRole
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: sts:AssumeRole
      Policies:
        - PolicyName: LambdaAccessPolicy
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action:
                  - dynamodb:PutItem
                  - dynamodb:GetItem
                  - dynamodb:Scan
                Resource: "*"
              - Effect: Allow
                Action:
                  - s3:PutObject
                  - s3:GetObject
                  - s3:ListBucket
                Resource: "*"
              - Effect: Allow
                Action:
                  - logs:CreateLogGroup
                  - logs:CreateLogStream
                  - logs:PutLogEvents
                Resource: arn:aws:logs:*:*:*

  SubmitVolunteerFormFunction:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: SubmitVolunteerForm
      Runtime: python3.11
      Role: !GetAtt LambdaExecutionRole.Arn
      Handler: volunteer_form.lambda_handler
      Code:
        S3Bucket: unitedrelief-volunteer-files
        S3Key: volunteer_form.zip
      Timeout: 30
      Environment:
        Variables:
          GMAIL_USER: almontesauel@gmail.com
          GMAIL_APP_PASSWORD: use-env-variable
          BUCKET_NAME: unitedrelief-volunteer-files

  SubmitReliefRequestFunction:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: SubmitReliefRequest
      Runtime: python3.11
      Role: !GetAtt LambdaExecutionRole.Arn
      Handler: relief_request.lambda_handler
      Code:
        S3Bucket: unitedrelief-volunteer-files
        S3Key: relief_request.zip
      Timeout: 30
      Environment:
        Variables:
          GMAIL_USER: almontesauel@gmail.com
          GMAIL_APP_PASSWORD: use-env-variable
          BUCKET_NAME: unitedrelief-volunteer-files

Outputs:
  VolunteerFunction:
    Value: !Ref SubmitVolunteerFormFunction
  ReliefFunction:
    Value: !Ref SubmitReliefRequestFunction
